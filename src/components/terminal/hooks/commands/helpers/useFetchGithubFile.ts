import { GithubFile } from "../../../models/githubFile";

export const useFetchGithubFile = () => {
  const fetchFile = async (): Promise<GithubFile | null> => {
    return null;
  };

  return { fetchFile };
};
